# v0.1.0 发布就绪（2026-07-22）

结论：无需且不应重切 v0.1.0；远端标签已指向 5575f6a，Release 已发布。该标签 Release、tag CI 及 main CI（含 dogfood）均成功；`dist/index.js` 有 provenance。package 版本为 0.1.0。release 由 `v*` 触发，含 `contents`、`attestations`、`id-token` 权限，配置正确。

阻塞后续发版：工作树有未提交 CI/源码/测试/文档；README 的 `<pinned-sha>` 示例不能直接运行。Dogfood 的 `@v0.1.0` 已通过，构建继续测当前源码、dogfood 测已发布制品；生产应固定 5575f6a… SHA，后续发版后以 PR 更新 pin。